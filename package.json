{
  "name": "audioconcat-security-patched",
  "version": "0.1.3",
  "description": "Concat multiple audio files into a unique one (uses ffmpeg)",
  "repository": "https://github.com/MrBoombastic/audioconcat-security-patched",
  "author": "Tomas Aparicio, MrBoombastic",
  "license": "MIT",
  "scripts": {
    "test": "./node_modules/.bin/mocha --timeout 300000 --reporter spec --ui tdd"
  },
  "keywords": [
    "audio",
    "mp3",
    "acc",
    "join",
    "concat",
    "merge",
    "song",
    "ffmpeg"
  ],
  "dependencies": {
    "fluent-ffmpeg": "^2.0.0",
    "lodash.merge": "^4.6.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^2.0.1"
  }
}
